---
applications:
  - name: nad-ch-dev
    buildpacks:
      - https://github.com/cloudfoundry/python-buildpack
    services:
      - nad-ch-identity-provider
      - nad-ch-dev-postgres
      - nad-ch-dev-redis
      - nad-ch-dev-s3
    random-route: false
    memory: 256M
    stack: cflinuxfs4
    command: python nad_ch/web.py
    env:
      APP_ENV: dev_remote
